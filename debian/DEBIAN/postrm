#!/usr/bin/env sh
set -eu

rm -f /etc/systemd/system/nut-counter-kiosk.service
rm -f /etc/systemd/system/nut-counter-backend.service
systemctl daemon-reload >/dev/null 2>&1 || true

if [ "${1:-}" = "purge" ]; then
  rm -rf /etc/nut-counter /var/lib/nut-counter /var/log/nut-counter
fi

exit 0
